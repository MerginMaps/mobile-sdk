The package GEODIFF provides CMake targets:

    find_package(GEODIFF REQUIRED)
    target_include_directories(main PRIVATE ${GEODIFF_INCLUDE_DIRS})
    target_link_libraries(main PRIVATE ${GEODIFF_LIBRARIES})
