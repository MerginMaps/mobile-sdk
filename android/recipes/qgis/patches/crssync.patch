--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -15,9 +15,9 @@ if (WITH_GUI)
 endif()
 
 add_subdirectory(providers)
-if (NOT IOS)
-  add_subdirectory(crssync)
-endif()
+#if (NOT IOS)
+#  add_subdirectory(crssync)
+#endif()
 
 if(WIN32 AND NOT MINGW)
   add_subdirectory(crashhandler)